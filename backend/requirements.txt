# LabhPay backend (FastAPI). Stage 1: minimum runnable surface.
# OCR/PDF/AI deps come online in later stages.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.10
httpx==0.27.2

# Auth / crypto
pyjwt[crypto]==2.9.0
cryptography==43.0.1
passlib[bcrypt]==1.7.4

# Redis (queue + encrypted temp store)
redis[hiredis]==5.0.8

# Celery (shared queue config import surface)
celery==5.4.0

# Supabase (metadata only)
supabase==2.7.4

# PDF probe (the API uses pymupdf just for the encryption probe; full
# extraction is the worker's job).
pymupdf==1.24.10

# PDF generation for the Stage 9 Export Center.
reportlab==4.2.5

# Google ID token verification (Stage 10.1 — Continue with Google).
google-auth==2.35.0
requests==2.32.3

# Observability / logging
structlog==24.4.0
